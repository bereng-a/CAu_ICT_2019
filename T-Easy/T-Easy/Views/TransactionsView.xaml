<UserControl x:Class="T_Easy.Views.TransactionsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid
          Margin="0,8,0,0"
          ItemsSource="{Binding Transactions}"
          CanUserSortColumns="True"
          CanUserAddRows="False" Height="376" VerticalAlignment="Top">
        </DataGrid>
        <Button Style="{StaticResource MaterialDesignRaisedLightButton}"
            ToolTip="Add User" Click="Button_Click" RenderTransformOrigin="3.946,6.862" Height="Auto" Margin="702,389,34,34">
            _ADD
        </Button>
    </Grid>
</UserControl>
